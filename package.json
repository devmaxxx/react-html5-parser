{
  "name": "react-html5-parser",
  "version": "0.1.0",
  "description": "Parse html into react",
  "main": "dist/index.js",
  "umd:main": "dist/index.umd.js",
  "module": "dist/index.m.js",
  "source": "src/index.ts",
  "scripts": {
    "dev": "microbundle watch -o dist/ --sourcemap false --no-compress",
    "size-check": "filesize",
    "clean": "rm -rf dist",
    "build:core": "npm run clean && npm run build:dist && npm run size-check",
    "build:lib": "microbundle --name index --no-sourcemap --generateTypes false --external react",
    "build:dist": "npm run build:lib -- --output dist",
    "build:debug": "npm run build:lib -- --output debug --no-compress",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "author": "devmaxxx",
  "license": "MIT",
  "devDependencies": {
    "@ampproject/filesize": "^4.3.0",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@types/dompurify": "^2.3.4",
    "@types/jest": "^29.2.0",
    "@types/react": "^18.0.22",
    "@types/react-dom": "^18.0.7",
    "@types/testing-library__jest-dom": "^5.14.5",
    "jest": "^29.2.1",
    "jest-environment-jsdom": "^29.2.1",
    "microbundle": "^0.15.1",
    "prettier": "^2.7.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "dompurify": "^2.4.0"
  },
  "engines": {
    "node": ">=14.16"
  },
  "jest": {
    "preset": "ts-jest",
    "setupFilesAfterEnv": [
      "<rootDir>/__tests__/setupTests.ts"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.ts?(x)"
    ],
    "testEnvironment": "jsdom"
  },
  "files": [
    "dist"
  ],
  "filesize": {
    "track": [
      "./dist/**/*.js"
    ]
  }
}
